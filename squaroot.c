#include<stdio.h>
#include<math.h>

int main(){

double squaroot,number;
printf("inter a number:");
scanf("%lf",&number);
squaroot=sqrt(number);
printf("squaroot of %lf =%lf",number,squaroot);
return 0;

}